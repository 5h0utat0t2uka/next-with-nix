{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "infisical run --env dev --path /next-with-nix -- dotenvx run -f .env.development -- next dev --hostname 0.0.0.0",
    "start": "infisical run --env prod --path /next-with-nix -- dotenvx run -f .env.production -- next start",
    "build": "infisical run --env prod --path /next-with-nix -- dotenvx run -f .env.production -- next build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "scan": "osv-scanner scan source -r ."
  },
  "dependencies": {
    "next": "^16.2.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.65.0",
    "@types/node": "^25.6.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3"
  }
}
